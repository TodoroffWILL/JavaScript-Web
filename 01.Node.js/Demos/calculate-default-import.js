const calculator = require('./calculator-default-export'); // Doesn't require to put the extension .js! The name doesn't matter as long as its from the currect file.

console.log(calculator.sum(2, 3));
console.log(calculator.multiply(5, 5));
